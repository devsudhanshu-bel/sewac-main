{
  "name": "sewac-admin-backend",
  "version": "0.1.0",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "postinstall": "npx prisma generate --schema=prisma/sewac.schema.prisma && npx prisma generate --schema=prisma/telemetry.schema.prisma && npx prisma generate --schema=prisma/cmads.schema.prisma && npx prisma generate --schema=prisma/helper.schema.prisma && npx prisma generate --schema=prisma/zone.schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^6.8.2",
    "axios": "^1.18.1",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.3",
    "node-cron": "^4.6.0",
    "pg": "^8.21.0",
    "redis": "^6.1.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.0",
    "prisma": "^6.8.2"
  }
}
